import React from 'react';
import EventDetails from '@/Components/Eventts/EventDetails';

const page = () => {
    return (
        <div className="bg-gradient-to-r from-black to-deep_blue ">
            <EventDetails/>
        </div>
    );
};

export default page;