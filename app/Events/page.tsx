import React from 'react';
import EventList from '@/Components/Eventts/EventList';
import { exploreWorlds } from '@/Constants';
import Events from '@/Components/Eventts/Events';

const page = () => {
    return (
        <div className="bg-gradient-to-r from-black to-deep_blue ">
            <Events/>
            
        </div>
    );
};

export default page;